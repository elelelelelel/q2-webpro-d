@{
    ViewBag.Title = "Home Page";
}

<div style="margin:auto; width: 100%; padding: 100px">
    <h1 style="text-align:center">Hidayah Library</h1>
    <hr />
    <div class="row" style="text-align:center; margin: auto">
        <h4 class="col-md-6">@Html.ActionLink("Add Book", "Create", "Menus")</h4>
        <h4 class="col-md-6 links">@Html.ActionLink("Book List", "Index", "Menus")</h4>
    </div>
